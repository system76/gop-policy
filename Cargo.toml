[package]
name = "system76_gop_policy"
version = "1.0.0"
edition = "2024"
license = "GPL-3.0-only"

[profile.release]
lto = true

[dependencies]
uefi = { version = "0.40", features = ["panic_handler"] }
uefi-raw = "0.16"
